## Playwright Target Rules
- Target browser engine is native Playwright.
- You MAY generate native Playwright pseudo-selectors such as `:has-text(...)`, `text=...`, or `:visible` for concise, robust element targeting.
