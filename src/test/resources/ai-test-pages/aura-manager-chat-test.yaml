data:
  - testId: "Chat_Message"
    before:
      - java startManager()
    steps: |
      Open ${auraManagerUrl} in the browser
      Enter "Hello Aura" in the chat input field
      Press the Enter key in the chat input field
      Verify that "Hello Aura" appears as a user message in the chat container
    after:
      - java stopManager()
