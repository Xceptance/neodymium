data:
  - testId: "History_List"
    before:
      - java startManager()
    steps: |
      Open ${auraManagerUrl} in the browser
      Click on the "Reports" navigation item 
      Verify that the Run History is displayed
    after:
      - java stopManager()
