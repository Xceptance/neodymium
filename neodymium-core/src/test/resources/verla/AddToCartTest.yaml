# AI-generated: Gemini 2.5 Pro
# @author AI-generated: Gemini 2.5 Pro
# @author Xceptance GmbH 2026

steps: |
  Open ${verla.url}/verla-${testId}/index.html
  Verify that the storefront home page has loaded.
  
  Locate the first product card:
    - Capture the cart line item count in 'lineItemCount'
    - Hover over it
    - Click its 'Add to Cart' button
    # Bad interface design has no size
    - When this string '${testId}' is not equal 'bad', click the size 'S'
    - Verify that the cart item count is higher than ${lineItemCount}.
  
  Locate the third product card:
    - Capture the cart line item count.
    - Hover over it
    - Click its 'Add to Cart' button
    # Bad interface design has no size
    - When this string '${testId}' is not equal 'bad', click the size 'L'
    - Verify that the cart item count is higher than before.

  Go to the cart by clicking the mini-cart icon.
  Verify that the shopping cart page is loaded and showing the added product.

  Locate the promo code input field:
    - type '10p-off' into it
    - Submit the form
    - Verify that the order summary now shows a 'Discount (10P-OFF)' line item.

  Locate the promo code input field:
    - clear its content
    - type 'FREEGIFT' into it
    - Submit the promo code form.
    - Assert that the cart items table contains a line item for 'Free Bonus Gift' (bug).

  Locate the country flag:
    - Click to open it
    - Type 'Sweden' into the country search field.
    - Select 'Sweden' from the results.
    - Verify that the page reloads or redirects, and prices/totals are now displayed in Swedish Krona 'kr' currency.

data:
  - testId: "perfect"
  - testId: "normal"
  - testId: "bad"
